name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]
jobs:
  deployment_pipeline:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Setup Node.js environment
        uses: actions/setup-node@v6.3.0
        with:
          node-version: "24"
      - name: Install dependencies
        run: npm ci
      - name: Check style
        run: npm run check
      # TODO: Write tests and test pipeline
      - name: Build
        run: npm run build
